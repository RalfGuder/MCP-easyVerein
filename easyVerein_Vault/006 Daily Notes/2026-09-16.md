# 2026-09-16

> **Branch:** main (+ PR-Branches `chore/api-v2-spec-refresh`, `fix/event-v2-removed-fields`)
> **Tests gesamt:** 234 grün (+10, Event-v2-Fix)
> **Endpoints gesamt:** 15
> **PRs gemerged:** #111 (Stadtfest-Kostenstellen), #112 (Vault-Umbenennung)
> **PRs offen:** #113 (v2.0-Spec-Refresh), #114 (Event-v2-Fix)
> **Fokus:** Repo-Pflege, Abgleich der API-v2.0-Spec, Event-Bugfix

## Ziele

- [x] Offene Konten.md-Änderung + Daily Notes committen, PR #111 mergen
- [x] Obsidian-Vault `docs/` → `easyVerein_Vault/` umbenennen (PR #112)
- [x] qmd-Collection für den Vault einrichten
- [x] Wissensstand zur API v2.0 prüfen
- [x] Event-Bug (v2.0) nach TDD beheben

## Vault-Umbenennung (PR #112)

- `git mv docs easyVerein_Vault`, Pfadverweise angepasst in `CLAUDE.md`, `.gitignore`, `.dockerignore`, `user-story`-Skill und Vault-Dokumenten.
- In `CLAUDE.md` stand ein falscher Pfad `docs/user stories/` → korrigiert auf `easyVerein_Vault/001 User Stories/`.
- **43 Issue-Bodies** nach dem Merge von `blob/main/docs/` auf `blob/main/easyVerein_Vault/` umgeschrieben.
- Merge-Konflikt „file location“: Die Daily Notes aus #111 lagen noch unter `docs/` → nach `easyVerein_Vault/006 Daily Notes/` verschoben.

## qmd

- Die Collection `docs` gehört **nicht** zu diesem Projekt (sie zeigt auf `convert_exe`). Der Vault war bisher nicht indiziert.
- Neue Collection **`easyverein-vault`** mit Kurzbeschreibung angelegt, Embeddings erzeugt.

## API v2.0 – Spec-Abgleich (PR #113)

Die lokale Spec stammte vom 2026-04-21. Die Live-Spec wurde mit `scripts/extract_openapi.py` neu erzeugt.

- `info.version` weiterhin `v2.0`; Pfade 517 → 521, keiner entfernt.
- 279 geänderte Pfade, **überwiegend kosmetisch**: Das Präfix „Zeichenwert;“/„Zahlenwert;“ und die Angabe „Standardwert: …“ fehlen jetzt in den Beschreibungen, dazu Tippfehler-Korrekturen.

Inhaltlich relevant:

| Endpoint | Änderung |
|---|---|
| event | **entfernt:** `parent`, `weekdays`, `massParticipations`; neu: `allowMultiplePriceGroupsPerParticipation`, `change_participation_state_to_unclear_when_event_moved` |
| contact-details | neu: `datev_creditor_number` |
| bank-account | neu: `creditor_address` |
| custom-filter | neu: `created_at`, `updated_at` → **für US-0020 berücksichtigen** |
| wastebasket | neu: `email-log`, `email-recipient`, `feedback`, `interval` |

Die Spec enthält **keine Response-Schemas** für GET. Ob Felder beim Lesen noch existieren, lässt sich nur live prüfen.

## Bug: list_events / get_event → HTTP 400 (PR #114)

Live-Probe mit den `FieldQuery`-Strings aller 15 Query-Klassen (per Reflection ausgelesen): **14× HTTP 200, nur `event` → 400**.

```
{id,parent}             400 ["'parent' field is not found"]
{id,weekdays}           400 ["'weekdays' field is not found"]
{id,massParticipations} 400 ["'massParticipations' field is not found"]
```

Fix nach TDD:
- Red: 9 Regressionstests (Theory über die 3 Felder) für den List-Selektor, den Get-Selektor und den Create-Body → 7 rot (beim Anlegen waren `parent`/`weekdays` als `null` ohnehin nicht im Body, `massParticipations` als `bool` dagegen immer).
- Green: Felder aus `EventQuery`, `Event` und `EventFields` entfernt; Domain-Test angepasst und ergänzt.
- 234 Tests grün. Live mit dem neuen Selektor: Liste und Einzelabruf liefern 200.

## Auffälligkeiten

- **Token-Rotation:** Der API-Token war erneut abgelaufen. Nach der Erneuerung in `.mcp.json` lieferte der **laufende** MCP-Server weiterhin 401. Der Server liest den Key nur beim Start → nach einem Token-Wechsel den MCP-Server neu starten.
- **Build-Sperre:** Der laufende MCP-Server (`dotnet run`) sperrt `src/*/bin`. Workaround für Tests: `dotnet test --artifacts-path <temp>`.
- Muster bestätigt: v2.0 prüft Feldnamen im `query=`-Selektor streng (vorher schon `accountingPlan`, `deleted`). Bei künftigen Spec-Änderungen alle `FieldQuery`s live testen.

## Nächste Schritte

- [ ] PR #113 + #114 mergen (CI abwarten)
- [ ] MCP-Server neu starten (neuer Token + Event-Fix), dann `list_events` per MCP testen
- [ ] Optional: neue Felder übernehmen (`datev_creditor_number`, `creditor_address`, Event-Neuerungen)
- [ ] US-0019 (Custom Field Collection), US-0020 (Custom Filter inkl. `created_at`/`updated_at`)
- [ ] Buchhaltungs-Rückstände aus [[2026-07-21]] (Getränkeerlöse Dorffest, PDF Beleg 491, Steuersätze 479/480, Konten-Duplikate)
