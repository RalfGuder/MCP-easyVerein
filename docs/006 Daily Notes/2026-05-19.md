# 2026-05-19

> **Branch:** feature/US-0016-contact-details-group @ `76fa725`
> **Tests gesamt:** 196 grün (Start: 185, +11: +2 Domain, +9 Infrastructure)
> **Endpoints gesamt:** 13 (main: 12; +1 ContactDetailsGroup auf Branch)
> **PRs offen:** 1 (#104)
> **PRs gemerged:** 0

## Ziele

- [x] US-0016 Contact-Details-Group-Endpoint implementieren (TDD)
- [x] User-Story-Doc 016 an US-0062 angleichen (Aufgabe `ApiQueries.cs erweitern` entfernen)
- [x] CLAUDE.md + Memory aktualisieren
- [x] PR gegen `main` öffnen

## Pull Requests offen

| PR | Titel | Status |
|---|---|---|
| [#104](https://github.com/RalfGuder/MCP-easyVerein/pull/104) | feat(US-0016): Contact-Details-Group-Endpoint implementieren | offen, wartet auf Review |

## Issues bearbeitet

- [#23 – US-0016 Contact-Details-Group-Endpoint implementieren](https://github.com/RalfGuder/MCP-easyVerein/issues/23) — PR #104 verlinkt

## Implementierungs-Details

- Entity-Felder: `id`, `name`, `color`, `short`, `orderSequence` (camelCase im API-Schema bestätigt durch `docs/api/easyverein-v2.0.yaml`)
- Filter-Set: `name`, `color`, `short`, `deleted`, `id__in`, `ordering`, `search` (search nur auf `name`/`short`)
- HTTP-Methoden: GET/POST/PATCH/DELETE (kein PUT API-seitig — Endpoint steht in "Nur PATCH"-Liste)
- Pagination: auto-alle-Seiten via `HandleListResponseWithPagination` (konsistent mit anderen Endpoints)
- Pattern: per-call Query-Instanzen gemäß US-0062 — keine Static-Singletons

## Buchungsregeln (Memory)

_(keine Änderungen)_

## Buchungen verbucht (Live)

_(keine)_

## Identifizierte offene Bugs

| Bug | Severity | Status |
|---|---|---|
| `ContactDetailsQuery.ToString()` Zeile 101 sendet `familyName=` statt `name=` | niedrig | offen (pre-existing, in US-0062 Task 3 entdeckt, orthogonal) |
| `get_booking` 404 nach `list_bookings` — Hypothese B (Soft-Delete) | hoch | wartet auf Live-Verifikation |

## Erkenntnisse

- **OpenAPI-Schema als Single Source of Truth**: `docs/api/easyverein-v2.0.yaml` lieferte alle Feld-, Filter- und Methoden-Infos für US-0016 ohne Live-Probe nötig. Spart Zeit gegenüber dem üblichen Trial-and-Error.
- **`orderSequence` ist camelCase**: Abweichung vom sonst dominierenden snake_case der easyVerein-API. Auch ChairmanLevel hat `short` und BillingAccount hat `defaultSphere`/`excludeInEur` als camelCase — Convention ist inkonsistent. Bei neuen Endpoints immer Schema prüfen, nicht annehmen.
- **Memory-Drift korrigiert**: `Server.Tests` ist nicht mehr leer wie in CLAUDE.md/Memory beschrieben — enthält 8 Tests (InvoiceToolsTests + PortoBuchungenPromptTests). Bei nächster Memory-Pflege ggf. Verifikation gegen Repo statt blindes Vertrauen auf Status-Memos.
- **Branch-Discipline**: `.claude/`-Untracked-Files konsequent außen vor gelassen — explizites Stagen statt `git add -A`.

## Nächste Schritte

- [ ] PR #104 mergen sobald Review/CI grün
- [ ] Live-Verifikation: `create_contact_details_group` → `get_contact_details_group` → `update_contact_details_group` → `delete_contact_details_group` gegen echte API
- [ ] Folge-Bug PR: `ContactDetailsQuery.ToString()` `familyName=` → `name=`
- [ ] Bug 2 (`get_booking` 404) live verifizieren — Hypothese B (Soft-Delete) entscheiden
- [ ] US-0017 Contact Details Log (Roadmap, PATCH-only)

## Tagesbilanz

| Kategorie | Wert |
|---|---|
| Commits auf Feature-Branch | 1 (`76fa725`) |
| LOC netto | +648 / -29 |
| Neue Files | 5 (Entity, ValueObject, Query, Tool, Domain-Test) |
| Modifizierte Files | 6 (Interface, ApiClient, Program.cs, Infra-Tests, CLAUDE.md, User-Story-Doc) |
| Tests neu | +11 (2 Domain + 9 Infrastructure) |
| Endpoints implementiert | 1 (ContactDetailsGroup, auf Branch) |
| PRs geöffnet | 1 (#104) |
